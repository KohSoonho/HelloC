/*
 At first, I make program that do nothing.
 C language are union of function.
 These function are not orderd, but `main` function are done at first.
 */

int main(void) {
    /* int = type, main = name of function, void = argument(this mean no arguments) */
	return 0;
    /*
     inside {} function body is written
     At C, to finish function I must write return sentense
     */
	}
