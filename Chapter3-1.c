#include <stdio.h>
/* 
To use `printf()` I must hand an operating files to compiler
`printf()` are include stdio.h file
*/

int main(void)
{
	printf("The pen is mighter \n than the sword \n");
	/* \n is regex to start a new line */
	return 0; 
	printf("Time is money");
	/* return finish function, and sentences after sentence are ignored. */
}

/*
Avobe function return
"The pen is migther
than the sword"
*/
